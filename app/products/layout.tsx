import type { ReactNode } from "react";

/** Product marketing pages use full-bleed TRAxCelerate-style layout */
export default function ProductsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
